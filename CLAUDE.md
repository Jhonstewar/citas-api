# CLAUDE.md — citas-api

Claude Code lee `CLAUDE.md`, no `AGENTS.md`. Este archivo importa el agente principal del backend
para que se cargue al trabajar en este repo:

@AGENTS.md

La gobernanza global y las reglas propias de Claude Code están en `../CLAUDE.md` y `../AGENTS.md`.
Especialistas de este repo: `backend-domain`, `backend-persistence`, `backend-security` y
`backend-verifier` (en `../.claude/agents/`).
